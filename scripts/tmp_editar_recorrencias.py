from pathlib import Path


def replace_once(text: str, old: str, new: str, label: str) -> str:
    if old not in text:
        raise SystemExit(f"Trecho não encontrado: {label}")
    if text.count(old) != 1:
        raise SystemExit(f"Trecho ambíguo ({text.count(old)} ocorrências): {label}")
    return text.replace(old, new, 1)


# ============================================================
# 1) Regras de atualização de recorrências
# ============================================================
path = Path("src/recurring_finance.py")
text = path.read_text(encoding="utf-8")

old_final = '''    if installment_number == total_installments:\n        total = _money(total_amount_raw)\n        previous = base * Decimal(max(total_installments - 1, 0))\n        last = (total - previous).quantize(MONEY, rounding=ROUND_HALF_UP)\n        return last if last > 0 else base\n    return base\n'''
new_final = '''    if installment_number == total_installments:\n        total = _money(total_amount_raw)\n        # A última parcela fecha exatamente o valor total do parcelamento.\n        # Isso continua correto mesmo quando o usuário edita valor/quantidade\n        # depois de algumas parcelas já terem sido geradas.\n        previous = Decimal("0.00")\n        recurring_id = str(plan.get("id") or "").strip()\n        user_id = str(plan.get("user_id") or "").strip()\n        if recurring_id and user_id:\n            rows = (\n                _client().table("transactions")\n                .select("amount")\n                .eq("user_id", user_id)\n                .eq("recurring_id", recurring_id)\n                .execute()\n                .data\n                or []\n            )\n            previous = sum((_money(row.get("amount") or 0) for row in rows), Decimal("0.00"))\n        else:\n            previous = base * Decimal(max(total_installments - 1, 0))\n        last = (total - previous).quantize(MONEY, rounding=ROUND_HALF_UP)\n        if last <= 0:\n            raise ValueError("O valor restante do parcelamento ficou inválido após a edição.")\n        return last\n    return base\n'''
text = replace_once(text, old_final, new_final, "ajuste da última parcela")

marker = '''\ndef set_recurring_active(user_id: str, recurring_id: str, active: bool) -> None:\n'''
update_function = '''\ndef _generated_amount_total(user_id: str, recurring_id: str) -> Decimal:\n    rows = (\n        _client().table("transactions")\n        .select("amount")\n        .eq("user_id", user_id)\n        .eq("recurring_id", recurring_id)\n        .execute()\n        .data\n        or []\n    )\n    return sum((_money(row.get("amount") or 0) for row in rows), Decimal("0.00"))\n\n\ndef update_recurring_plan(\n    user_id: str,\n    recurring_id: str,\n    *,\n    account_id: str,\n    category_id: str | None,\n    description: str,\n    next_due_date: date,\n    notes: str = "",\n    monthly_amount: float | None = None,\n    total_amount: float | None = None,\n    total_installments: int | None = None,\n) -> dict[str, Any]:\n    """Edita somente a configuração futura de uma recorrência do próprio usuário.\n\n    Lançamentos já materializados permanecem intactos. Para parcelamentos já\n    iniciados, o valor das parcelas futuras é recalculado sobre o saldo que\n    ainda falta gerar, preservando o total informado pelo usuário.\n    """\n    if not description.strip():\n        raise ValueError("Informe uma descrição.")\n\n    client = _client()\n    rows = (\n        client.table("recurring_transactions")\n        .select(\n            "id,user_id,account_id,category_id,kind,description,amount,frequency,next_due_date,is_active,"\n            "schedule_type,start_date,due_day,total_amount,total_installments,generated_installments,end_date,notes"\n        )\n        .eq("id", recurring_id)\n        .eq("user_id", user_id)\n        .limit(1)\n        .execute()\n        .data\n        or []\n    )\n    if not rows:\n        raise ValueError("Recorrência não encontrada.")\n\n    plan = rows[0]\n    schedule_type = str(plan.get("schedule_type") or SCHEDULE_FIXED)\n    generated = int(plan.get("generated_installments") or 0)\n    due_day = int(next_due_date.day)\n\n    payload: dict[str, Any] = {\n        "account_id": account_id,\n        "category_id": category_id,\n        "description": description.strip(),\n        "next_due_date": next_due_date.isoformat(),\n        "due_day": due_day,\n        "notes": notes.strip() or None,\n    }\n\n    if schedule_type == SCHEDULE_FIXED:\n        amount = _money(monthly_amount or 0)\n        if amount <= 0:\n            raise ValueError("Informe um valor mensal maior que zero.")\n        payload.update(\n            {\n                "amount": float(amount),\n                "total_amount": None,\n                "total_installments": None,\n                "end_date": None,\n            }\n        )\n    elif schedule_type == SCHEDULE_INSTALLMENTS:\n        installments = int(total_installments or 0)\n        current_total_installments = int(plan.get("total_installments") or 0)\n        completed = current_total_installments > 0 and generated >= current_total_installments\n\n        if completed:\n            # Parcelamento concluído: somente dados descritivos podem ser corrigidos.\n            payload.update(\n                {\n                    "amount": float(_money(plan.get("amount") or 0)),\n                    "total_amount": float(_money(plan.get("total_amount") or 0)),\n                    "total_installments": current_total_installments,\n                    "end_date": plan.get("end_date"),\n                }\n            )\n        else:\n            if installments < max(generated + 1, 1) or installments > 360:\n                raise ValueError(\n                    f"Informe entre {max(generated + 1, 1)} e 360 parcelas; "\n                    f"{generated} já foram geradas."\n                )\n            total = _money(total_amount or 0)\n            if total <= 0:\n                raise ValueError("Informe o valor total do parcelamento.")\n\n            already_generated = _generated_amount_total(user_id, recurring_id)\n            remaining_count = installments - generated\n            remaining_total = (total - already_generated).quantize(MONEY, rounding=ROUND_HALF_UP)\n            if remaining_total <= 0:\n                raise ValueError(\n                    "O novo valor total precisa ser maior que o valor das parcelas já geradas."\n                )\n            parcel = (remaining_total / Decimal(remaining_count)).quantize(MONEY, rounding=ROUND_HALF_UP)\n            if parcel <= 0:\n                raise ValueError("O valor das próximas parcelas ficou inválido.")\n\n            payload.update(\n                {\n                    "amount": float(parcel),\n                    "total_amount": float(total),\n                    "total_installments": installments,\n                    "end_date": add_months(\n                        next_due_date,\n                        remaining_count - 1,\n                        due_day=due_day,\n                    ).isoformat(),\n                }\n            )\n    else:\n        raise ValueError("Modalidade recorrente inválida.")\n\n    response = (\n        client.table("recurring_transactions")\n        .update(payload)\n        .eq("id", recurring_id)\n        .eq("user_id", user_id)\n        .execute()\n    )\n    updated_rows = response.data or []\n    return updated_rows[0] if updated_rows else {**plan, **payload}\n\n'''
if marker not in text:
    raise SystemExit("Ponto de inserção de update_recurring_plan não encontrado")
text = text.replace(marker, update_function + marker, 1)
path.write_text(text, encoding="utf-8")


# ============================================================
# 2) Interface de edição no Planejamento de Caixa
# ============================================================
path = Path("pages/Planejamento_Caixa.py")
text = path.read_text(encoding="utf-8")

old_import = '''    materialize_due_recurring,\n    set_recurring_active,\n)'''
new_import = '''    materialize_due_recurring,\n    set_recurring_active,\n    update_recurring_plan,\n)'''
text = replace_once(text, old_import, new_import, "import update_recurring_plan")

management_marker = '''\ndef _render_management() -> None:\n'''
editor = '''\n@st.dialog("✏️ Editar conta / recorrência", width="large")\ndef _edit_recurring_dialog(recurring_id: str) -> None:\n    if recurring is None or recurring.empty:\n        st.error("Recorrência não encontrada.")\n        return\n\n    matches = recurring[recurring["id"].astype(str) == str(recurring_id)]\n    if matches.empty:\n        st.error("Recorrência não encontrada.")\n        return\n\n    selected = matches.iloc[0]\n    kind = str(selected.get("kind") or "despesa")\n    schedule_type = str(selected.get("schedule_type") or SCHEDULE_FIXED)\n    generated = int(selected.get("parcelas_geradas") or 0)\n    total_existing = int(selected.get("parcelas_total") or 0) if pd.notna(selected.get("parcelas_total")) else 0\n    completed = schedule_type == SCHEDULE_INSTALLMENTS and total_existing > 0 and generated >= total_existing\n\n    account_map = _account_options()\n    category_map = _category_options(kind)\n    if not account_map or not category_map:\n        st.error("Conta ou categoria necessária para edição não está disponível.")\n        return\n\n    account_labels = list(account_map.keys())\n    current_account_id = str(selected.get("account_id") or "")\n    current_account_label = next(\n        (label for label, value in account_map.items() if str(value) == current_account_id),\n        account_labels[0],\n    )\n\n    category_labels = list(category_map.keys())\n    current_category_id = str(selected.get("category_id") or "")\n    current_category_label = next(\n        (label for label, value in category_map.items() if str(value) == current_category_id),\n        category_labels[0],\n    )\n\n    next_due = selected.get("proximo_vencimento")\n    if not isinstance(next_due, date):\n        raw_next = selected.get("next_due_date")\n        next_due = pd.to_datetime(raw_next).date() if pd.notna(raw_next) else date.today()\n\n    st.caption(\n        "As alterações passam a valer para as próximas ocorrências. "\n        "Lançamentos que já foram gerados permanecem no histórico e podem ser corrigidos em Lançamentos."\n    )\n    st.info(\n        f"Modalidade: **{selected.get('modalidade')}** • "\n        f"Tipo: **{selected.get('tipo')}**. A modalidade é preservada para proteger o histórico."\n    )\n\n    c1, c2 = st.columns(2)\n    with c1:\n        description = st.text_input(\n            "Descrição",\n            value=str(selected.get("description") or ""),\n            key=f"edit_recurring_description_{recurring_id}",\n        )\n        account_label = st.selectbox(\n            "Conta de movimentação",\n            account_labels,\n            index=account_labels.index(current_account_label),\n            key=f"edit_recurring_account_{recurring_id}",\n        )\n    with c2:\n        category_label = st.selectbox(\n            "Categoria",\n            category_labels,\n            index=category_labels.index(current_category_label),\n            key=f"edit_recurring_category_{recurring_id}",\n        )\n        next_due_date = st.date_input(\n            "Próximo vencimento / recebimento a gerar",\n            value=next_due,\n            key=f"edit_recurring_next_due_{recurring_id}",\n        )\n\n    monthly_amount = None\n    total_amount = None\n    total_installments = None\n\n    if schedule_type == SCHEDULE_FIXED:\n        monthly_amount = st.number_input(\n            "Valor mensal",\n            min_value=0.01,\n            value=float(selected.get("valor_ciclo") or selected.get("amount") or 0.01),\n            step=10.0,\n            format="%.2f",\n            key=f"edit_recurring_monthly_{recurring_id}",\n        )\n        st.success("🔁 O novo valor será usado a partir da próxima ocorrência ainda não gerada.")\n    else:\n        if completed:\n            st.warning(\n                "Este parcelamento já foi concluído. Você pode corrigir descrição, conta, categoria e observação, "\n                "mas valor e quantidade de parcelas ficam preservados."\n            )\n            total_amount = float(selected.get("valor_total") or 0)\n            total_installments = total_existing\n        else:\n            p1, p2 = st.columns(2)\n            with p1:\n                total_amount = st.number_input(\n                    "Valor total do parcelamento",\n                    min_value=0.01,\n                    value=float(selected.get("valor_total") or 0.01),\n                    step=10.0,\n                    format="%.2f",\n                    key=f"edit_recurring_total_{recurring_id}",\n                )\n            with p2:\n                minimum_installments = max(generated + 1, 1)\n                total_installments = int(\n                    st.number_input(\n                        "Quantidade total de parcelas",\n                        min_value=minimum_installments,\n                        max_value=360,\n                        value=max(total_existing, minimum_installments),\n                        step=1,\n                        key=f"edit_recurring_installments_{recurring_id}",\n                    )\n                )\n            st.info(\n                f"🧾 **{generated} parcela(s) já gerada(s)**. "\n                "O RENOVA recalculará somente as próximas parcelas e ajustará a última por centavos, se necessário."\n            )\n\n    raw_notes = selected.get("notes")\n    notes = st.text_input(\n        "Observação (opcional)",\n        value="" if pd.isna(raw_notes) else str(raw_notes or ""),\n        key=f"edit_recurring_notes_{recurring_id}",\n    )\n\n    if st.button(\n        "💾 SALVAR ALTERAÇÕES",\n        key=f"save_recurring_edit_{recurring_id}",\n        type="primary",\n        use_container_width=True,\n    ):\n        try:\n            update_recurring_plan(\n                user_id,\n                str(recurring_id),\n                account_id=account_map[account_label],\n                category_id=category_map[category_label],\n                description=description,\n                next_due_date=next_due_date,\n                notes=notes,\n                monthly_amount=monthly_amount,\n                total_amount=total_amount,\n                total_installments=total_installments,\n            )\n            st.success("Recorrência atualizada. As próximas ocorrências usarão os novos dados.")\n            st.rerun()\n        except Exception as exc:\n            st.error(f"Não foi possível salvar a edição: {exc}")\n\n'''
if management_marker not in text:
    raise SystemExit("Ponto de inserção do editor não encontrado")
text = text.replace(management_marker, editor + management_marker, 1)

old_controls = '''    c1, c2 = st.columns([1, 2])\n    with c1:\n        action = "⏸️ Pausar" if active else "▶️ Reativar"\n        if st.button(action, use_container_width=True, key="toggle_recurring_active"):\n            try:\n                set_recurring_active(user_id, str(selected["id"]), not active)\n                st.success("Recorrência atualizada.")\n                st.rerun()\n            except Exception as exc:\n                st.error(f"Não foi possível atualizar: {exc}")\n    with c2:\n        if selected.get("schedule_type") == SCHEDULE_INSTALLMENTS:\n            remaining = int(selected.get("parcelas_restantes") or 0)\n            st.info(f"Restam **{remaining} parcela(s)** para este lançamento.")\n        else:\n            st.info("Esta recorrência mensal continua até ser pausada pelo usuário.")\n'''
new_controls = '''    c1, c2, c3 = st.columns([1, 1, 2])\n    with c1:\n        action = "⏸️ Pausar" if active else "▶️ Reativar"\n        if st.button(action, use_container_width=True, key="toggle_recurring_active"):\n            try:\n                set_recurring_active(user_id, str(selected["id"]), not active)\n                st.success("Recorrência atualizada.")\n                st.rerun()\n            except Exception as exc:\n                st.error(f"Não foi possível atualizar: {exc}")\n    with c2:\n        if st.button("✏️ Editar", use_container_width=True, key="edit_recurring_plan"):\n            _edit_recurring_dialog(str(selected["id"]))\n    with c3:\n        if selected.get("schedule_type") == SCHEDULE_INSTALLMENTS:\n            remaining = int(selected.get("parcelas_restantes") or 0)\n            st.info(f"Restam **{remaining} parcela(s)** para este lançamento.")\n        else:\n            st.info("Esta recorrência mensal continua até ser pausada pelo usuário.")\n'''
text = replace_once(text, old_controls, new_controls, "controles de gerenciamento")
path.write_text(text, encoding="utf-8")

print("OK: edição de contas fixas e parceladas implementada")
